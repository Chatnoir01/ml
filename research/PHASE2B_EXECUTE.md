# Phase 2B — Scientific execution marker

AUTHORIZED_PHASE2B_GA_ORACLE_PRESSURE

## Frozen pre-marker scientific source

`49f9df6ed9c243ebd81adc77039cfd502515c09b`

This execution marker is committed only after the Phase 2B execution lock in issue #98 was satisfied at the pre-marker source above.

Verified before authorization:

- Phase 0 CI GREEN on Python 3.10, 3.11, and 3.12;
- historical Phase 1 benchmark GREEN;
- historical Phase 2A-U2 qualification regression retained and unchanged;
- Phase 2B protocol and pre-execution amendments/clarifications frozen in issues #97, #101, and #102;
- RED→GREEN contracts for protected-key-only Oracle pressure, negative-control shuffling, matched budgets, held-out isolation, receipts, and classical protection;
- nine Phase 2B evolution seeds centrally reserved and proven fresh;
- held-out Block-V neural seeds centrally reserved and proven fresh against prior neural registries and the U2 fitness block;
- exact classical budget frozen at 340 unique full evaluations per arm/seed;
- exact fitness-Oracle budget frozen at 32 candidate scores = 512 neural trainings per arm/seed;
- each Oracle candidate score retains a complete 16-training payload with fingerprint, seeds, pair count 8192, split 5734/1228/1230, and SHA-256 receipt;
- Arm C replays the confirmed Phase-1O fighter semantics;
- Oracle pressure cannot cross exact `primary_security_key` equality;
- Block V is absent from the evolutionary runner and is evaluated only after terminal freeze;
- shuffled negative control uses one persistent RNG initialized from `evolution_seed + 10000`;
- aggregate validation verifies exact budgets, receipts, held-out identity, classical non-degradation, and the preregistered six support criteria;
- aggregate is required to be produced twice and byte-identical;
- PR diff inspected and restricted to Phase 2B plumbing, tests, registries, workflow, protocol, and this execution marker;
- no Phase 2B neural-pressure scientific result was observed before this authorization;
- full adaptive GA↔NN co-evolution remains prohibited in Phase 2B.

## Scientific freeze rule

The Git commit containing this marker is the authoritative Phase 2B scientific execution SHA.

No threshold, evolution seed, neural seed, arm, budget, Oracle regime, endpoint, held-out block, or interpretation rule may be changed based on Phase 2B outcomes. Any implementation or provenance failure must invalidate or require a separately preregistered successor experiment rather than silently modifying this run.

## Scope

Educational/defensive ToySPN research only. This phase does not establish weakness in AES or any deployed cipher and does not authorize operational cryptanalytic exploitation.
