name: Phase 1M DU Hotspot Development

on:
  push:
    branches:
      - research/phase1m-du-hotspot-bridge
    paths:
      - research/PHASE1M_EXECUTE.md

permissions:
  contents: read

jobs:
  preflight:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install package
        run: python -m pip install -e .
      - name: Verify frozen Phase 1M design and execution lock
        run: |
          python - <<'PY'
          from pathlib import Path
          from adversarial_sbox.experiment_seeds import (
              PHASE1M_CONFIRM_RESERVED_SEEDS,
              PHASE1M_DEV_SEEDS,
              validate_seed_registry,
          )
          from adversarial_sbox.phase1m import (
              CLASSICAL_BUDGET,
              GENERATIONS,
              ITO_NONINFERIORITY_TOLERANCE,
              PARENT_COUNT,
              POPULATION_SIZE,
              PROPOSALS_PER_GENERATION,
              PROPOSALS_PER_PARENT,
              SHORTLIST_SIZE,
          )

          validate_seed_registry()
          assert PHASE1M_DEV_SEEDS == (2111, 2113, 2129, 2131, 2137)
          assert PHASE1M_CONFIRM_RESERVED_SEEDS == (
              2203, 2207, 2213, 2221, 2237, 2239, 2243, 2251, 2267
          )
          assert CLASSICAL_BUDGET == 340
          assert POPULATION_SIZE == 20
          assert SHORTLIST_SIZE == 8
          assert PARENT_COUNT == 4
          assert PROPOSALS_PER_PARENT == 4
          assert PROPOSALS_PER_GENERATION == 16
          assert GENERATIONS == 20
          assert ITO_NONINFERIORITY_TOLERANCE == 0.02
          assert Path('research/PHASE1M_PROTOCOL.md').is_file()
          marker = Path('research/PHASE1M_EXECUTE.md')
          assert marker.is_file()
          assert 'AUTHORIZED_PHASE1M_SCIENTIFIC_RUN' in marker.read_text()
          print('Phase 1M frozen preflight OK')
          PY
      - name: Record scientific SHA
        run: git rev-parse HEAD

  develop:
    needs: preflight
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        seed: [2111, 2113, 2129, 2131, 2137]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install package
        run: python -m pip install -e .
      - name: Run frozen Phase 1M seed with deterministic rerun
        run: |
          python -m adversarial_sbox.phase1m \
            --seeds ${{ matrix.seed }} \
            --output phase1m-seed-${{ matrix.seed }}.json
      - name: Show compact seed summary
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          seed = os.environ['SEED']
          data = json.loads(Path(f'phase1m-seed-{seed}.json').read_text())
          compact = {
              'seed': data['seed'],
              'best_du_a': data['arm_a']['best_du'],
              'best_du_b': data['arm_b']['best_du'],
              'du8_a': data['arm_a']['du8_count'],
              'du8_b': data['arm_b']['du8_count'],
              'protected_a': data['arm_a']['protected_classical_count'],
              'protected_b': data['arm_b']['protected_classical_count'],
              'min_ito_a': data['arm_a']['min_ito'],
              'min_ito_b': data['arm_b']['min_ito'],
              'budgets': [
                  data['arm_a']['classical_evaluations'],
                  data['arm_b']['classical_evaluations'],
                  data['arm_c']['classical_evaluations'],
              ],
              'deterministic': data['deterministic_payload_match'],
              'payload_sha256': data['scientific_payload_sha256'],
              'neural_oracle_executed': data['neural_oracle_executed'],
          }
          print(json.dumps(compact, indent=2, sort_keys=True))
          PY
        env:
          SEED: ${{ matrix.seed }}
      - name: Upload seed evidence
        uses: actions/upload-artifact@v4
        with:
          name: phase1m-seed-${{ matrix.seed }}-${{ github.sha }}
          path: phase1m-seed-${{ matrix.seed }}.json
          if-no-files-found: error
          retention-days: 90

  aggregate:
    needs: develop
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install package
        run: python -m pip install -e .
      - name: Download seed evidence
        uses: actions/download-artifact@v4
        with:
          pattern: phase1m-seed-*-${{ github.sha }}
          path: cells
          merge-multiple: true
      - name: Aggregate frozen development result
        run: |
          python -m adversarial_sbox.phase1m \
            --aggregate-files cells/phase1m-seed-*.json \
            --output phase1m-development.json
      - name: Show aggregate verdict
        run: |
          python - <<'PY'
          import json
          data = json.load(open('phase1m-development.json'))
          print(json.dumps(data['summary'], indent=2, sort_keys=True))
          print(json.dumps(data['development_checks'], indent=2, sort_keys=True))
          print('VERDICT:', data['verdict'])
          PY
      - name: Upload aggregate evidence
        uses: actions/upload-artifact@v4
        with:
          name: phase1m-development-summary-${{ github.sha }}
          path: phase1m-development.json
          if-no-files-found: error
          retention-days: 90
