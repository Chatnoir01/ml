name: Phase 1B V2 Confirmation

on:
  push:
    branches:
      - research/phase1b-feasibility-first
    paths:
      - '.github/workflows/phase1b-confirmation.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  confirm:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout frozen V2 experiment
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install package
        run: python -m pip install -e .
      - name: Run preregistered V2 confirmation
        run: python -m adversarial_sbox.phase1b_confirmation --output phase1b-confirmation.json
      - name: Show preregistered verdict
        if: always()
        run: |
          if [ -f phase1b-confirmation.json ]; then
            python -c "import json; d=json.load(open('phase1b-confirmation.json')); print('verdict=', d['verdict']); print(json.dumps(d['gates'], indent=2, sort_keys=True)); print(json.dumps(d['evidence']['summary'], indent=2, sort_keys=True))"
          fi
      - name: Upload V2 confirmation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase1b-confirmation-${{ github.sha }}
          path: phase1b-confirmation.json
          if-no-files-found: error
          retention-days: 90
